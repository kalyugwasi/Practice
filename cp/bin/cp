#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
if [[ "${1:-}" == "run" ]]; then
  shift
  exec python3 "$script_dir/../python/run.py" "$@"
fi
exec python3 "$script_dir/../judge/cfjudge.py" "$@"
