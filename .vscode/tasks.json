{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CP: run samples",
            "type": "shell",
            "command": "${workspaceFolder}/cp/bin/cp",
            "args": [
                "run"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            }
        }
    ]
}
